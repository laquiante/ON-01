---
- hosts: leaf
  strategy: free
  become: true
  user: cumulus
  gather_facts: false
      
  roles:
    - /home/cumulus/ON-01/unit-07-role/roles/leaf_l3if
    
- hosts: spine
  strategy: free
  become: true
  user: cumulus
  gather_facts: false
      
  roles:
    - /home/cumulus/ON-01/unit-07-role/roles/spine_l3if
     
- hosts: host
  strategy: free
  become: true
  user: cumulus
  gather_facts: false
  tasks:
    - name: copy netplan yaml
      copy:
        src: /home/cumulus/ON-01/inventory/files/{{ inventory_hostname }}-unit-07-l3if
        dest: /etc/netplan/config.yaml
    - name: apply netplan yaml
      shell: netplan apply

- hosts: leaf11
  become: true
  user: cumulus
  gather_facts: false
  tasks:
    - name: patch vrf and sub-interfaces
      copy:
        src: /home/cumulus/ON-01/unit-07-role/playbooks/leaf11_patch
        dest: /tmp/leaf11_patch.yaml
    
    - name: patch changes
      shell: nv config patch /tmp/leaf11_patch.yaml
      
    - name: activate changes
      shell: nv config apply -y
